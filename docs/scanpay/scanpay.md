# 掃碼支付

Demo 只保留會員端掃描、付款與產生 QR Code 的操作，以及完成付款所需的訂單對應與狀態處理。

後台查詢／統計／匯出／取消與收銀台建立、查詢訂單 API 均已移除。

因收銀台建立來源單的 API 已移除，初始化資料會提供一筆金額 `100` 的固定來源單與 QR Code，供會員端掃描及付款流程展示。付款成功後資料會轉為已完成；重新建立 MySQL demo volume 即可重測。

實際可呼叫路由請見[前台 API](../app_apis_doc.md)。
